bitcoin push-mining pool server.

Dependencies:
	libevent		Event-driven async I/O
	libmemcached		memcached client
	pthreads		POSIX threads
	zlib			gzip compression
	jansson			JSON
	OpenSSL's libcrypto	sha256
	libcurl			HTTP client



Added to original version 

* any_password accept and password logging
* log requests to mysql 
* attach own pool to BIG one

